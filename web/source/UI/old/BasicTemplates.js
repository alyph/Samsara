UI.Template(
["grid", { $container: true }
]);

UI.Template(
["label", { $element: UI.Label }
]);

UI.Template(
["image", { $element: UI.Image, $container: true }
]);

UI.Template(
["list", { $element: UI.List }
]);

